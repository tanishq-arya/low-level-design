// This interface defines the flyweight methods for displaying characters with shared attributes.

package FlyWeightPattern.Exercise;

interface CharacterFlyweight {
    void display(String character);
}